# What is the DOM

DOM (Document Object Model) is a programming interface that represents an HTML document as a structured tree of objects.

When a web page loads in the browser, the browser converts the HTML into the DOM. Each element (like <div>, <p>, <h1>) becomes a node in this tree.

DOM is what allows JavaScript to make web pages interactive and dynamic instead of static.